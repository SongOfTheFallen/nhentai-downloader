version: '3'
services:
  frontend:
    build:
      context: .
      args:
        VITE_API_BASE_URL: ${VITE_API_BASE_URL}
        VITE_API_PASSWORD: ${VITE_API_PASSWORD}
    ports:
      - "8787:80"
    env_file:
      - .env
